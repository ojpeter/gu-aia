# GU-AIA — Progress Log

**Last updated:** 2026-08-31

**Resume here — ALL TWELVE INVARIANTS PASS, AND THE CONSOLE IS FUNCTIONALLY COMPLETE EXCEPT FOR RE-INDEXING.** 339 tests / 972 assertions, PHPStan level 8 clean, PSR-12 clean, 37/37 grant probes. Screens: dashboard, curated answers, authoritative sources.

**Next: the crawler and PDF extractor** — the last major unbuilt piece, and the one that finally lets the eval harness measure retrieval instead of reporting 35 questions as PENDING. **Phase 0 gates it**: the machinery may be built, but nothing may be indexed until Communications and the Registry have assigned an authoritative source, an owner and a review date per fact. Re-indexing in the console is really this piece's prerequisite rather than a console gap.

**Next:** the two remaining console write actions — marking a document authoritative (permission, 2FA gate and audit action already exist and are tested; only the screen is missing) and triggering a re-index (which needs the ingester, so it is really the crawler's prerequisite). After that, the crawler and PDF extractor, which **Phase 0 gates**.

**Next:** the console's three write actions — curated Q&A authoring, marking a document authoritative, triggering a re-index. The permissions, the 2FA gate and the audit actions for all three already exist and are tested; only the screens are missing, and the dashboard lists them as not built rather than showing disabled buttons.

**Read this before telling anyone the system is safe, because it is not the same claim.** Every invariant has a mechanism and a test *for that mechanism*. Several also have a behavioural half that only the evaluation harness can measure — does the model actually obey the prompt, does retrieval actually refuse when it should — and the harness cannot measure any of it without a corpus, which Phase 0 gates. 35 of the 118 golden questions still report PENDING for exactly that reason. The honest sentence is *“the controls exist and are proven to work as designed”*, not *“the assistant has been shown to behave well on real questions”*. The status page says this too, deliberately.

**Next:** the admin console (Section 14) or the crawler and PDF extractor — but note the crawler is the piece that *could* index content, and **Phase 0 must complete first**. `composer ci` runs the whole gate: PHPStan level 8 clean, PSR-12 clean, **279 tests / 716 assertions** green, 29/29 grant probes, and `bin/evaluate.php` over 118 golden questions — **83 evaluated and passing, 0 failing, 35 correctly reported PENDING**. 22 tables across 8 forward-only migrations, three least-privilege accounts. The corpus is deliberately **empty** and must stay empty until Phase 0 completes. **Why the eval suites are still PENDING even though retrieval now exists:** the retriever is built but not wired into the harness, the threshold is deliberately `null` (it fails closed until tuned against the eval set), and the corpus is empty because Phase 0 gates indexing. Do not add `retrieval` to `stages_built` in `config/eval.php` until the pipeline runs end to end against real content. Still missing: the crawler and PDF extractor, the admin console, and the retention sweep. **The assistant currently refuses every question**, correctly and by design: the corpus is empty and the retrieval threshold is `null`, so `Retriever` fails closed with `retrieval_threshold_not_configured`. That is the honest behaviour of a system with nothing to answer from, and it is what a visitor sees today. Status page at `http://localhost/gu-aia/`; repo at `https://github.com/ojpeter/gu-aia.git`.

**Three things the harness says that must not be smoothed over.** (1) The golden set is **118 of the 200** Section 12 requires, and the missing 82 are exactly the part only the Registry and Communications can write — the ordinary programme, admissions and contact questions they know the public actually asks. (2) **0 of 118 questions have been authored by an office.** Section 12 says the set is authored *with* them; none of it has been. (3) **Acholi and Luganda are absent.** They are not stubbed on purpose: inventing questions in a language without a competent speaker would put wrong-language strings into the one artefact meant to tell the truth about language quality. Until those rows exist, Section 18 open question 3 stands and **no language but English may be advertised.**

**Read this before touching the schema — a real trap, found the hard way:** `NOT NULL` on a `DATE` column is **not** a guarantee on a MySQL-family server that is not in strict mode. The XAMPP MariaDB 10.4 default (`sql_mode` without `STRICT_TRANS_TABLES`) silently substitutes `0000-00-00` and accepts the row. A test insert omitting `reviewed_at` succeeded and produced exactly the row INV-11 exists to make impossible — a document with a zero review date, which would be served as freshly reviewed and would never trip the stale-content caution. Closed at two layers: `CHECK` constraints in `0007_enforce_review_dates.sql` (evaluated regardless of `sql_mode`) and a strict `sql_mode` set on every connection in `config/pdo_options.php`. **This will recur**: development is MariaDB 10.4, production targets MySQL 8, and their defaults differ — so where an invariant depends on a column genuinely holding a real value, say so with a `CHECK`, not just `NOT NULL`.

**Standing gate, unchanged:** Phase 0 (the content audit) gates **indexing**, not schema. The schema exists; nothing may be indexed until Communications and the Registry have assigned an authoritative source, an owner and a review date per fact.

**Previous resume note (superseded, kept for context):** The repository was foundations only — created as a new sibling project at `C:\xampp\htdocs\gu-aia` alongside `gu-website` and `gu-services`, with `requirements.md` (the client-supplied 19-section engineering contract, verbatim), `CLAUDE.md` (13 project rules encoding the AI-governance and data-access standards), the three standards artefacts under `docs/`, the Section 4 directory skeleton, a forward-only migration runner, configuration stubs, and the PHPUnit/PHPStan/Composer scaffolding — with no schema, no ingestion, no retrieval, no answering, no widget and no tests.

---

## The Rules in Brief (full text in `CLAUDE.md` — mirrored here so this file is self-contained)

1. **The twelve invariants are law.** Each needs a named test in `tests/Invariant/`. Never weaken one for a feature; never add a bypass path.
2. **AI governance by named standard**: `DICTS/POL/AI/001` governs and outranks the rest; NIST AI RMF 1.0 for risk shape; ISO/IEC 42001 + 23894 for lifecycle; EU AI Act Art. 50 as a transparency benchmark (not a compliance claim); honest capability claims always.
3. **Data access**: purpose limitation (no Phase 1 personal-data surface at all), one least-privilege DB account per role with grants verified functionally, prepared statements everywhere including the `FULLTEXT` clause, explicit column whitelists, no hard deletion, DPPA 2019 for the chat logs, secrets out of the repo, corpus minimisation, TLS.
4. **LLM security to OWASP Top 10 for LLM Apps**: prompt injection defended in three places, model output treated as untrusted on render, no agency, rate limits + budget cap, no internal detail rendered, provider isolated behind `Generator`.
5. **App security to OWASP ASVS**: server-side validation, `finfo` MIME re-validation on upload, output encoding, CSRF, admin auth + per-action authz, security headers tested in the embedded case, no error detail to visitors, append-only admin audit log.
6. **Retrieval discipline**: retrieval is expected to refuse; category filtering is a safety control; quoted mode never generates the figure; never split a fees table; `reviewed_at` mandatory; conflicts are content defects.
7. **Evaluation is a gate**: harness built in the first sprint, ≥200 golden questions authored with Registry + Communications, runs in CI and blocks merges, re-run after every re-index. Never fabricate an eval number.
8. **Accessibility WCAG 2.1 AA + 60 KB widget budget + no-JS fallback** as a first-class path.
9. **Logging**: full Section 13 field list under one correlation ID, prompt version recorded, weekly Unanswered Questions Report as a primary deliverable.
10. **Code quality**: PSR-12/PSR-4, PHPStan clean, forward-only migrations, no `DELETE`, working fake `Generator` so everything is testable with no spend.
11. **Standards maintenance is continuous**: `docs/standards.md`, `docs/ai-risk-register.md`, `docs/data-protection.md` updated on events, not on a calendar; re-check standards at each phase gate and record the check even when nothing changed.
12. **`progress.md` every session** — read before starting, update before stopping.
13. **Honesty about state**: distinguish *specified* / *implemented* / *verified*. Only the third is done.

---

## Completed Tasks Log

- **2026-08-31** — **Project created as a new sibling at `C:\xampp\htdocs\gu-aia`.** Client instruction arrived mid-session during the `gu-website` git push; confirmed with the client that GU-AIA is its own project and its own repository (not a `gu-website` subdirectory), consistent with `requirements.md` Section 0 calling this file "the engineering contract for this repository", Section 3's separate `gu_aia` MySQL schema, and Section 4's own repository layout. Scope for this first pass confirmed as **foundations only**. *(Sections 0, 3, 4)*
- **2026-08-31** — `requirements.md` added verbatim as supplied by the client — 19 sections, the twelve invariants, hybrid-retrieval decision, corpus rules, answer categories, prompt contract, refusal/handoff, interface, safety/cost, eval harness, logging, admin console, phases, definition of done, out-of-scope, open questions. Not paraphrased or edited. *(all sections)*
- **2026-08-31** — `CLAUDE.md` written: 13 project rules. Rules 2, 3 and 4 are the direct response to the client's standing instruction to *"implement and maintain industry standards on AI and Data Access"* — AI governance mapped to named external frameworks, data access mapped to least-privilege/purpose-limitation/DPPA controls, and LLM-specific security mapped to the OWASP Top 10 for LLM Applications. *(Sections 2, 3, 11)*
- **2026-08-31** — `docs/standards.md` created — the standards register: every control mapped to the external standard it derives from, to where it will be implemented, and to its test. **Every row honestly marked `Specified`**, since no code exists; the three-state vocabulary (Specified / Implemented / Verified) is defined at the top so a later session cannot quietly mark something done because it is written down. *(Sections 2, 11)*
- **2026-08-31** — `docs/ai-risk-register.md` created — NIST AI RMF MAP+MEASURE artefact, 13 risks identified (R-1 fabricated fees figure through R-13 Phase-2 surface added early), each with impact, likelihood, designed treatment, and how it will be measured. All treatments marked "Not implemented". *(Section 11, NIST AI RMF)*
- **2026-08-31** — `docs/data-protection.md` created — DPPA 2019 data-flow table, 7 flows (DF-1 interaction log through DF-7 query text sent to the generation API), each with lawful basis, retention and access. Records explicitly that **chat logs are personal data even with no login**, that retention values are not yet set (open question 5), and that cross-border transfer must be stated plainly rather than assumed away (open question 2). *(Sections 13, 18)*
- **2026-08-31** — Section 4 directory skeleton created (`bin/`, `config/prompts/`, `db/migrations/`, `public/`, `src/{Ingestion,Retrieval,Answering,Safety,Logging,Admin}/`, `templates/`, `tests/{Unit,Integration,Invariant,Eval}/`, `docs/`), plus `storage/{logs,cache,corpus}/`. *(Section 4)*
- **2026-08-31** — `bin/migrate.php` written — real, working, forward-only migration runner: reads `.env` with no dependency, connects under the dedicated `DB_MIGRATION_USER` account, creates the `schema_migrations` ledger on first use, applies pending `.sql` files in filename order each in its own transaction, stops on first failure. **Enforces INV-12 in code, not by review**: refuses any migration containing `DELETE FROM` or `TRUNCATE`. *(Sections 3, 2/INV-12)*
- **2026-08-31** — Configuration stubs written, each carrying the specification's own reasoning as comments so a later session cannot mistake a placeholder for a tuned value: `config/retrieval.php` (threshold deliberately `null` and marked NOT TUNED), `config/categories.php` (the Section 7 category→mode routing), `config/budget.php` (ceiling `null` — must fail closed to retrieval-only, never fail open), `config/corpus.php` (**deliberately empty of real sources — Phase 0 gates indexing**), `config/refusals.php` (**placeholder copy only; Section 9 requires Communications to author refusal text, and contact details left `null` so the renderer fails loudly rather than publishing a wrong office email**), `config/prompts/README.md` (versioning rules + the Section 8 prompt contract). *(Sections 5, 6, 7, 8, 9, 11)*
- **2026-08-31** — **The project now serves at `http://localhost/gu-aia/`** (client instruction). A root `.htaccess` rewrites every request into `public/`, so `config/`, `src/`, `db/`, `docs/`, `storage/`, `bin/` and `.env` are not web-reachable — routing and defence-in-depth in one file, plus `Options -Indexes` and a deliberately tight CSP (this project serves no third-party assets at all). `public/index.php` is an honest **status page**, not a widget: it states plainly that there is no assistant yet, quotes Section 0's failure-mode framing, lists the twelve invariants as specified-but-unimplemented, and runs a real environment preflight (PHP version, required extensions, `.env`, DB reachability, migration count, generator driver). **Deliberately not built: anything resembling a chat interface** — a widget shell with no retrieval behind it would be a system that appears to answer and cannot, which is the exact failure this project exists to prevent. Preflight detail is gated on `APP_ENV=development`, since a preflight table is a reconnaissance aid (Rule 5). Verified: `php -l` clean on all 7 PHP files, `/` returns `200`, and — checked, not assumed — `/.env` returns `403`, `/requirements.md` returns `403`, `/config/budget.php` returns `404`. *(Sections 3, 4, 10; CLAUDE.md Rule 5)*
- **2026-08-31** — **First browser pass with Claude in Chrome (new standing client directive), and it immediately found a real bug.** The status page's "Foundations only" badge was rendering **white text on the yellow accent (`#fff24d`) — roughly 1.1:1, a flat WCAG failure and a direct breach of the brand rule that yellow pairs with dark text only.** Cause: `header.masthead p` (specificity 0,1,2) beat `.badge` (0,1,0), so the dark color I had set never applied despite appearing later in the file. **`curl` could not have caught this** — it confirms the CSS text is served, not which rule wins the cascade. This is the same class of bug that bit `gu-website` twice (see that project's `progress.md`). Fixed with a higher-specificity `header.masthead p.badge` selector and a comment explaining why, then re-verified by zooming in on the rendered badge. Also confirmed in-browser: page renders correctly, preflight table populates and reports honestly (`.env present: Pending`, `Database: skipped — no .env`, `Migrations: 0 written`), zero console errors. **Not verified:** narrow-viewport rendering — `resize_window` changed window height but not the rendered viewport width on this machine (screenshots stayed ~1568px at requested widths of 390px and 500px), so the mobile layout is confirmed only by reading the CSS (fluid units, `clamp()` heading, `auto-fill minmax` grid, `overflow-x: auto` on the table), **not by eye**. *(Standing directive; CLAUDE.md Rule 8, Rule 13)*
- **2026-08-31** — Page content width set to **80% of the viewport, centred** (client instruction), replacing the `62rem` max-width band. Added a `max-width: 48rem` guard widening it to 92% on phones, since 80% of a 390px screen leaves ~312px of text — the 80% band is the desktop intent, and breaking mobile readability to honour it literally would trade one instruction against the standing mobile-responsiveness directive. Verified in-browser at desktop width. *(Section 10)*
- **2026-08-31** — **Marking a source authoritative built (Sections 5.2, 14)** — the highest-consequence action in the system. It does not change what a document says; it changes which one the assistant quotes when two disagree, which for the fees category decides the figure a member of the public is shown. Two properties carry the weight. **Exactly one authoritative source per category**, enforced in the same transaction: two would make the outcome depend on retrieval order, which is precisely the ambiguity the flag exists to remove, and it would do so invisibly because both rows would look correct in isolation. And **the flag propagates to the chunks**, which denormalise it — a document flagged without its chunks updated would be authoritative in the console and not in the answering pipeline, while the screen showed the change had worked. That is the quiet failure this action could have had. *(Section 5.2)*
- **2026-08-31** — The screen is built so the consequence is visible **before** the click: each category shows what is currently authoritative, and the confirmation names what was displaced. The audit entry names **both** documents, so “why did the assistant quote the wrong figure” is answerable a year later from one line. Permission is checked at the page guard **and again inside `AuthoritativeMarker`** — a privileged action that trusts its caller to have checked is one refactor away from being unguarded. A superseded document is refused (the flag would resurrect retired content) and so is one filed under a different category (the category filter and the flag must not disagree). *(Rule 5)*
- **2026-08-31** — Verified in the running console, not just by test: signed in as an **editor**, requested the authoritative screen, got **403**, and confirmed the denial was written to the audit log as `permission_denied` naming the permission. Then deleted the account and the audit rows and re-checked the corpus at zero. Console navigation now shows each screen only to the roles that may use it. *(Section 14)*
- **2026-08-31** — **Curated question-and-answer authoring built (Sections 5.1, 14)** — the one thing the console may author. A curated entry is backed by real `documents` and `chunks` rows **with an embedding**, so it flows through the same retrieval, citation and `reviewed_at` machinery as crawled content. Deliberately not a special case: a hand-written answer is not more trustworthy than a published page, and a shortcut around the citation and staleness rules would make the console the one place unverifiable facts could enter. The chunk carries the **question as well as the answer**, because retrieval matches on the chunk body and the public phrases queries far closer to the question — a chunk holding only the answer would be near-unfindable by the query it was written for. *(Sections 5.1, 14)*
- **2026-08-31** — **Editing supersedes; it does not overwrite (INV-12), and the form says so on screen.** Overwriting a curated answer would destroy the text somebody may already have been answered from — silently, through the most ordinary action in the console. An edit writes a new document, chunk and entry and marks the previous ones superseded; the old chunk is deactivated first, so no window exists where two versions are both live and competing for the same question. Tested. *(INV-12)*
- **2026-08-31** — **A fourth database account, `gu_aia_console`, and the reasoning matters more than the account.** Section 14 says the console authors curated entries, which ARE corpus content — but `gu_aia_app` is deliberately unable to write the corpus, and that absence is verified. Those are not in conflict once the distinction is named: the risk being guarded against is an **unauthenticated request path** writing published content, and an authenticated, permission-checked, audited console action is a different thing. Widening the app's grants would have quietly dissolved a control I had proven; a separate account keeps it intact and provable. Scoped tightly: it may write curated entries and their backing rows, may **read** the reports Section 14 requires it to show but not edit them, cannot change a role or password hash, and has no `DELETE`. 37 probes now, all passing. *(CLAUDE.md Rule 3; Section 14)*
- **2026-08-31** — Validation is a **type, not a check** (Rule 4): `CuratedEntryInput::fromRequest()` returns either a valid object or a map of field errors, so the writer cannot be handed something unvalidated because there is no way to express it. Enum-like fields are checked against the real `offices` and `categories` tables rather than a hard-coded array — otherwise a stale option in the form becomes a foreign-key 500 instead of a field error somebody can act on. Dates are parsed, not regexed (`2026-02-30` passes any sensible pattern and is not a date), and a **future** review date is refused because it would keep the staleness sweep permanently satisfied — INV-11 defeated by a typo. *(Rule 4)*
- **2026-08-31** — **My own grant probe was wrong and the harness caught it.** The first “console may author” probe used an INSERT that violated a NOT NULL constraint, so it reported `error` rather than `allowed` — exactly the state `verify_grants.php` distinguishes on purpose, because a probe that fails for a non-privilege reason is not testing what it claims. Replaced with a no-op UPDATE, where privilege is checked before the row is built. *(Rule 13)*
- **2026-08-31** — Verified end to end through the real form: signed in, submitted an entry **with no review date and watched it be refused**, then submitted a valid one and confirmed a `curated` document, an active chunk with a 1024-byte embedding, and an audit row naming the actor. Then deleted all of it and re-checked that documents, chunks, curated entries and admin users are all back to zero — the corpus must stay empty until Phase 0.
- **2026-08-31** — **TOTP secrets encrypted at rest, closing the gap flagged the same day rather than building more surface over it.** `Admin\SecretBox` wraps AES-256-**GCM** under a new `SECRET_ENCRYPTION_KEY`. GCM rather than CBC or CTR because the authentication tag matters as much as the confidentiality here: a TOTP secret an attacker can flip bits in is a TOTP secret they can grind. The key is kept **separate from `LOG_HASH_KEY`** on purpose — that one pseudonymises log identifiers and may reasonably be readable by whoever runs reports, while this one guards a second factor, and reusing one key across both purposes makes the weaker handling of either the security of both. The envelope carries a version byte so the algorithm can change later without a migration having to guess how existing rows were written. *(Rule 5)*
- **2026-08-31** — **The negative tests are the point.** A tampered ciphertext, a tampered tag, a wrong key, an unknown envelope version, a truncated envelope and — the migration trap — **legacy plaintext** must all fail to decrypt. Accepting a plaintext row “for compatibility” would quietly undo the encryption for every row that had not yet been rewritten, which is exactly how this kind of change gets silently reverted. A row that will not decrypt is treated as a missing secret, so the account simply cannot sign in. Also asserted that the envelope fits `VARBINARY(255)`, since a silently truncated write would fail to decrypt afterwards and look like a lost secret. *(Rule 13)*
- **2026-08-31** — Verified end to end, not just by unit test: created a real authoriser account with an encrypted secret, confirmed sign-in is **refused without a code and accepted with one**, confirmed the signed-in user may mark a document authoritative, and confirmed **the stored column does not contain the plaintext secret** (61 bytes of envelope). Then deleted the account and re-checked that `admin_users` is back to zero and the corpus is still empty. *(Standing verification practice)*
- **2026-08-31** — **Admin console: security core and read-only dashboard (Section 14).** `Role` (reader / editor / authoriser), `AuthenticatedUser`, `Authenticator`, `Totp`, `AuditLog`, `ConsoleContext`, plus `bin/create_admin.php` and the login, dashboard and logout pages. **The ladder is deliberately steep at the top**: only `mark_authoritative` requires 2FA, because it is the action that decides which source wins a conflict (Section 5.2) and therefore which fees figure a member of the public is shown. **Having the authoriser role and having passed 2FA *this session* are kept as two separate facts** — conflating them is how a 2FA requirement quietly becomes a label. And there is no `edit_corpus` permission to grant, with a test asserting no role has one: Section 14 says “the console must never become a second place where facts live”. *(Section 14; CLAUDE.md Rule 5)*
- **2026-08-31** — **The sign-in failure path is where most of the care went.** Unknown email, wrong password, deactivated account, temporary lockout and a missing second factor all return the **same** result and the same message — staff emails at a university are guessable by construction, so a helpful error turns the form into an account enumerator. A bcrypt hash is computed even when the account does not exist, because otherwise response time distinguishes “no such user” from “wrong password” just as loudly as a different message would. Lockout is **temporary**, since a permanent one hands an attacker a denial-of-service against a named member of staff for the price of five wrong guesses. Failed sign-ins are audited **without a user id** even when the email matched: attributing them would put “somebody tried to sign in as this named person” into a table other staff read, on the strength of an attempt anyone can manufacture. *(Rule 5)*
- **2026-08-31** — TOTP implemented rather than pulled in: sixty lines of HMAC, an algorithm unchanged since 2011, and a dependency in the authentication path is a supply-chain surface on the one route that protects everything else. Constant-time comparison (six digits is a small enough space that timing matters), and exactly **one** step of clock drift either way — phones drift and a staff member forty seconds fast should not be locked out, but every extra window multiplies the codes an attacker can guess at once. Includes an **RFC 4226 known-answer test**, which is the one that matters: a self-consistent implementation can be self-consistently wrong, and this proves it interoperates with a real authenticator app. *(Rule 5, Rule 10)*
- **2026-08-31** — **PHPStan caught a design smell and I fixed the design, not the annotation.** The console bootstrap left `$pdo`, `$csrf`, `$audit`, `$esc` and `$require` lying in the including scope; analysis objected that they “might not be defined”. It was right: a page depending on variables a `require` happens to leave behind has a dependency nobody can see, and renaming one breaks pages silently at runtime rather than loudly at analysis time. Replaced with a `ConsoleContext` returned from the bootstrap. Also caught a bug of my own during the rewrite — a closure that ended up referencing `$console` from outside its scope. *(Rule 10)*
- **2026-08-31** — The dashboard puts the **unanswered-question report first**, above the corpus browser, because Section 13 calls it a primary deliverable; leading with the corpus would make this a content-management screen with a report attached, which is the wrong way round. It also shows the refusal-reason tally separately, and states plainly that an empty corpus **is correct** rather than showing zeros without explanation. The three unbuilt write actions are **listed as not built** rather than rendered as disabled buttons, which would imply they nearly work. *(Sections 13, 14)*
- **2026-08-31** — Verified by creating a temporary local account, signing in over curl, confirming the dashboard rendered, then **deleting the account and its audit rows**. Re-checked afterwards that `admin_users` is back to zero and the corpus is still empty. The login page was checked in a real browser; the dashboard was not, because Chrome autofilled the user's own saved credentials into the form and submitting somebody's real password is not something to do in passing. *(Standing browser-check directive)*
- **2026-08-31** — **INV-11 tested, completing all twelve.** `ReviewedAtMandatoryTest` covers both halves, because either alone is worthless: a system that stores review dates faithfully and never shows them tells nobody anything, and one that renders a date it never enforced renders a zero. The schema half pins the original bug — a document with no review date, a `0000-00-00` date, a zero interval, or no owning office are each refused — and the render half asserts the caution appears **above** the answer, not below it, since a warning a reader meets after the figure has already been read is not a warning. Also asserts a fresh source produces **no** caution: over-warning is its own failure, because a caution on every answer is one nobody reads by the third. *(INV-11)*
- **2026-08-31** — A fixture of mine had to be rewritten mid-test: I tried `COALESCE(:reviewed_at, DEFAULT(reviewed_at))` to express “no review date”, which fails because `reviewed_at` deliberately has **no default** — a defaulted review date is a fabricated review date. The fixture now omits the column entirely, which is also the real-world shape of the bug: ingestion code that simply forgets the field. Removed a tautological assertion I had written (`x < x + 1`), which proved nothing. *(Rule 13)*
- **2026-08-31** — **Retention sweep built (INV-12, DPPA), closing the gap that shipping the logger created.** `RetentionSweeper` + `bin/redact_expired.php` expire personal data by **redaction**: the row survives with its correlation ID, timings, mode, category and refusal reason intact — so “on this date the assistant refused this category of question” stays answerable a year later — while the query text, the answer and any free-text feedback are blanked. Deleting the row would satisfy a naive reading of data protection and destroy exactly what this system needs when something goes wrong. Technical identifiers (hashed IP, session) expire on their own **shorter** clock per DF-2. `redacted_at` makes a second sweep a no-op. *(Section 13; INV-12)*
- **2026-08-31** — **The sweep refuses to run without a configured period, and exits non-zero.** `LOG_RETENTION_DAYS` is still unset (Section 18, open question 5). Guessing would be worse than failing: too short destroys the record a complaint needs, too long is a retention nobody authorised. A nightly job that fails loudly until somebody sets a number is the correct alarm for a gap that would otherwise sit unnoticed for a year — and it is now a live gap rather than a future one, because both the log writer and the widget exist. Three new grant probes confirm the **app** account (which is what the sweep runs as) may redact by UPDATE on all three tables. *(DPPA 2019; DF-1, DF-2)*
- **2026-08-31** — **The widget is built and works end to end (INV-4, INV-9).** `public/ask.php` renders the shell on GET and answers on POST, and **the no-JavaScript path is the primary one** — Section 10 requires a plain form posting to the same endpoint, and the surest way to keep a fallback working is to make it the path everyone uses rather than a second implementation nobody exercises. `widget.js` is enhancement over a page that already works, and hands the submission back to the browser on any failure. Verified end to end over curl: CSRF token issued, question posted with no JavaScript at all, pipeline answered, interaction logged, transaction committed, cited HTML returned. A post with no token returns **400**; the disclosure is present even on that error page. *(Sections 10, 11)*
- **2026-08-31** — **`ask.php` owns the transaction the logger's contract depends on** (INV-7): begin, answer, log, commit, then render. A response is never sent before its log entry is committed, and a throw rolls the whole thing back. This closes the pending item from the previous session — the logger writing "in the caller's transaction" only means anything once a caller actually owns that boundary. *(Section 13)*
- **2026-08-31** — **A real fabrication bug, found by looking at the rendered page and by nothing else.** The widget told a visitor asking about fees to "contact **Finance Department**" — an office `requirements.md` never names and which may not exist under that title. I had already refused to seed it into the `offices` table for exactly that reason (see `0006_seed_reference_data.sql`), and then left it sitting in `config/refusals.php`, where it reached a rendered page. **No test caught it and none would have**, because it is not a code defect: it is a fabricated fact, rendered confidently, of precisely the kind this project exists to prevent. Fixed by nulling the office so the renderer falls back to a phrase that names nobody and sets `handoffMissing`. The other three contacts were cross-checked against `requirements.md` and are genuinely named there. **This is the single best argument for the standing browser-check directive that this project has produced so far.** *(Section 9; CLAUDE.md Rule 13)*
- **2026-08-31** — Safety layer for the endpoint: `Csrf` (`hash_equals`, and the question form counts as state-changing because it writes a row, consumes rate-limit budget and will eventually spend money), `RateLimiter` (fixed-window **upsert**, so two concurrent requests cannot both read 9 and both write 10; violations logged, never a silent 429) and `RateLimitDecision` (carries the limit and the retry window, because a breach message that cannot say when to try again is not the "clear message" Section 11 asks for). Both scopes are limited: per-session alone is defeated by dropping the cookie, and per-IP alone throttles a whole campus behind one NAT address — which in Uganda is the common case, not the edge case. *(Section 11)*
- **2026-08-31** — **Starter questions deliberately not built.** Section 10 says they are "drawn from the top real questions in the log, not invented". There are no real questions yet, so inventing plausible ones would put fabricated demand data in front of Communications and would quietly become the thing everyone clicks, which then becomes the log. The renderer accepts them and passes none. *(Section 10)*
- **2026-08-31** — **Interaction logger built (INV-7), with the transaction boundary that the invariant actually turns on.** `InteractionLogger` writes `interactions`, `interaction_retrievals` (every chunk retrieval returned, with its score and whether it reached the answer), `interaction_citations` and — on every refusal — `unanswered_questions`, all **inside the caller's transaction**. It opens none of its own. That is the whole point of Section 13's wording: log-after-respond loses exactly the interactions most worth having, the ones where something went wrong mid-request, which are the ones a complaint will be about. There is now no state in which a visitor received an answer the University has no record of. A test proves it by rolling back and asserting the row is gone — if the logger had committed independently, it would survive. *(Section 13; INV-7)*
- **2026-08-31** — **The IP hash is keyed, and an unkeyed fallback is refused outright.** A plain SHA-256 of an IPv4 address is not pseudonymisation: the whole space is ~4.3 billion values, minutes of brute force, so anyone holding the log recovers every address exactly. `IdentifierHasher` uses HMAC under `LOG_HASH_KEY` and **throws if the key is absent** rather than degrading to a plain hash — a fallback would produce a column that looks pseudonymised, passes review, and is trivially reversible, which is the worst of the three states because nobody would know to look again. An absent IP stores `NULL`, not a hash of the empty string. `docs/data-protection.md` DF-2 updated to match, with the reasoning recorded. *(DPPA 2019; DF-2)*
- **2026-08-31** — `UnansweredQuestionsReport` built: ranked by frequency, grouped by category, and **also grouped by refusal reason** — which is the half worth reading separately. A week dominated by `below_threshold` says the corpus is thin or the threshold is wrong; a week dominated by `individual_outcome` says the public is asking something the assistant will never answer and the Registry may want a clearer page about it. Those need completely different responses, and a single ranked list of questions hides the difference. Redacted rows are excluded, since reporting an empty string as a top question would be worse than omitting it. *(Section 13 — a primary deliverable)*
- **2026-08-31** — **The schema caught a fixture bug, correctly.** The first logger test failed on a foreign-key violation: `interaction_retrievals.chunk_id` references `chunks`, and the test was logging against invented ids. The refusal is right — a citation pointing at a chunk that never existed would make INV-1's audit trail worthless — so the test now seeds a real document and chunk **inside the transaction it rolls back**. Verified afterwards that `documents`, `chunks`, `interactions` and `unanswered_questions` are all still at zero rows: the corpus must stay empty until Phase 0, and a fixture that leaked would put fabricated content into the very corpus this project exists to keep honest. Rollback is also the only cleanup available, since no account holds `DELETE` — the constraint and the testing strategy agree. *(INV-1, INV-12)*
- **2026-08-31** — **The versioned prompt and the answering pipeline.** `config/prompts/system-v1.txt` carries all eight of Section 8's required instructions (plus a refusal clause), and `ClosedRetrievalScopeTest` asserts each clause is literally present — a prompt is the easiest artefact in a codebase to erode, one tightening edit at a time, and these assertions make that a failing build. `PromptBuilder` fences and numbers every passage and **strips the fence delimiter from passage bodies**, so a document carrying the closing delimiter cannot end the data block and continue as though it were the system speaking — the same class of bug as SQL injection, with the same fix: the untrusted value must not reach the syntax. `AnsweringPipeline` is where the invariants hold or fail, and the **order of its five steps is the safety property**: route before retrieving (INV-3), refuse on no confident context (INV-1), quoted mode never reaches the generator (INV-2), budget and timeout guard the call (INV-8), citations bound with failures discarded and never repaired (Section 8). `BudgetGuard` **fails closed on a null ceiling**, which means degraded mode is the DEFAULT path until somebody authorises spend — so it is exercised constantly rather than being a branch first taken in production during an admissions cycle. *(Sections 6, 7, 8, 9, 11)*
- **2026-08-31** — **Corrected an earlier decision of mine, in writing rather than quietly.** `config/refusals.php` says the handoff renderer should "fail loudly on a null contact rather than emit a refusal with no route". Right instinct, wrong mechanism: throwing would mean that until the Registry supplies an email address, every refusal — the correct outcome for a large share of questions — becomes an error page, and the user gets nothing at all. That is strictly worse than a refusal naming an office without a phone number. The refusal is now always rendered and `handoffMissing` is set, so the gap is loud to the people who can fix it and never loud at the person who just asked a question. The pre-launch checklist keeps it from shipping unresolved. *(Section 9)*
- **2026-08-31** — Extracted `ContextRetriever` so the pipeline can be tested against fixed context. Not only a testing convenience: every invariant the pipeline carries is about what happens AFTER retrieval, and tying them to a live corpus would mean they could not be tested until Phase 0 completes. `tests/Support/PipelineBuilder` assembles the **real** pipeline — real router, real binder, real refusal config with its contacts still null — substituting only the retriever and the generator. *(Rule 13)*
- **2026-08-31** — **Deliberately did NOT add `ingestion`, `prompt` or `binder` to `stages_built` in `config/eval.php`, even though all three now exist with passing tests.** The harness still evaluates the routing layer only. Running it through the pipeline against an empty corpus would make every question refuse — turning the out-of-corpus and injection suites GREEN for entirely the wrong reason while the precision cases went red. A suite that passes because the system can answer nothing at all is exactly the false green the harness exists to prevent. The reasoning is recorded in `config/eval.php` beside the setting. *(Section 12; Rule 13)*
- **2026-08-31** — **Ingestion built.** `Cleaner` (INV-6), `Chunker` (Section 5.3), `Embedder` interface + `HashingEmbedder`, plus `Block`/`ExtractedDocument`/`Chunk` value objects. **The cleaning design is the part to read**: it splits into two operations with different risk profiles. Artefacts with no legitimate place in published prose — invisible and bidirectional control characters, HTML comments, script/style blocks, `data:` URIs — are **removed outright**; a university page never needs a zero-width joiner mid-sentence, and if one is there it is there to hide text from a human reviewer while the model still reads it. But instruction-shaped *natural language* is **flagged and kept, never deleted**: "Applicants should ignore the previous instructions on the old form" is a sentence a real page could contain, and silently deleting it would change what the University said — this system exists to report published content faithfully, and a corpus quietly edited by a regex is no longer that content. INV-6 is defended instead by delimiting, the prompt contract and the citation binder; cleaning is the layer that should be least willing to alter text. `Chunker` enforces the atomic-block rule as a **safety** rule: half a fees table is not a smaller answer but a wrong one — it looks complete, cites cleanly, and quotes an amount belonging to a different programme, which is risk R-1 arriving past INV-2 because the figure was never generated, it was retrieved. *(Sections 5.2, 5.3; INV-6)*
- **2026-08-31** — **Retrieval built.** `QueryNormaliser`, `CandidateGenerator`, `Reranker`, `VectorCodec`, `Retriever`, `RetrievalResult`. **The highest-risk query in the system is handled at two levels, and the second is the one people skip**: the `FULLTEXT ... AGAINST` expression is bound (which stops injection completely) *and* has every BOOLEAN MODE operator stripped, because a bound parameter is still parsed by MySQL as a search expression — a user typing `-fees tuition` has bound a perfectly safe parameter that instructs the engine to **exclude every document mentioning fees**, from a question plainly about fees. That is not injection; it is a stranger steering retrieval, and binding does nothing about it. `LIMIT` is cast and clamped because it cannot be bound under native prepares. **`Retriever` fails closed on an unset threshold**: `score_threshold` is `null` until tuned against the eval set, and a null threshold refuses everything with reason `retrieval_threshold_not_configured` — an untuned threshold means nobody knows at what score an answer stops being supported, and a system that does not know when to refuse must refuse. *(Section 6; CLAUDE.md Rule 3)*
- **2026-08-31** — **Two design defects found by the new tests, both real.** (1) The exact-code boost was **multiplicative**, which cannot lift a near-zero base — a chunk that exactly matched a typed course code could still be outranked by prose merely mentioning the programme, defeating Section 6's "a user typing a code knows what they want". Changed to **additive and at least the maximum base score** (the weights sum to 1.0), making exact-code matches a strict tier rather than a nudge. (2) The instruction-shape pattern for "from now on" required a following pronoun and missed the commonest phrasing; broadened, which is safe here precisely because matching only *flags*. Also fixed five PHPStan level-8 list-type findings rather than suppressing them. *(Section 6; INV-6)*
- **2026-08-31** — On the embedder, stated plainly rather than glossed: `HashingEmbedder` is a **lexical** embedder, not a semantic one. It captures spelling overlap and morphology and nothing about meaning — it will not connect "fees" to "how much do I pay". It exists because Section 18 open question 1 is open and the alternatives were worse: a zero-returning stub would make the rerank silently inert while looking implemented, and wiring a hosted embedding API would take a spend-and-data-residency decision that belongs to the Chief, ICT Services. Before launch, run the harness with this and with whichever semantic embedder is chosen, and compare — the number comes from the harness, not from an assumption. `modelName()` is versioned so a swap forces a re-index rather than mixing incompatible vectors, and `CandidateGenerator::embeddings()` refuses to load vectors from a different model. *(Sections 3, 18)*
- **2026-08-31** — **Evaluation harness built (Section 12: "build it in the first sprint", and it was).** `bin/evaluate.php` runs the golden set, records an `eval_runs` row plus per-question `eval_results`, reports the four Section 12 metrics, and exits non-zero on regression so CI can gate on it (`composer ci` = analyse, lint, test, eval). `bin/seed_eval_questions.php` seeds from `config/eval/golden_set.php`, is idempotent, and **never touches a row an office owns** — a seeder that silently reverted the Registry's edits would be worse than no seeder. Migration `0008` made `expected_mode` nullable and added `must_not_refuse`, because the precision block has no single correct mode ("what are the entry requirements" is legitimately Quoted, "how do I apply" legitimately Grounded; what matters is that neither is refused), plus a `source` column so the harness can report how much of the set an office has actually authored. 118 questions seeded: the 40 individual-outcome phrasings, 20 high-stakes, 15 injection, 20 out-of-corpus, 8 individual-record and 15 precision cases. **Two of the four metrics report NOT MEASURABLE rather than a number** — retrieval hit rate and citation validity need a corpus, and a harness that printed "citation validity: 100%" when nothing was cited would be worse than one that printed nothing, because somebody would quote it. *(Section 12, 14)*
- **2026-08-31** — **Corrected my own harness design after its first run, and the correction is the interesting part.** The first run reported **34 failures** across the out-of-corpus and injection suites. The expectations were right — both blocks must end in a refusal — but that refusal comes from *retrieval* finding nothing above threshold (INV-1) and from the prompt contract and citation binder (INV-6), none of which is built; the router's Grounded routing was correct. Reporting them as failures is dishonest in the opposite direction from the usual one: it makes an unbuilt system look broken and trains whoever reads the output to ignore red. Suites now declare which pipeline stages their expectation depends on (`suite_requires` in `config/eval.php`) against a `stages_built` list, and report **PENDING** — not passed, not failed, excluded from the metric denominators, and listed with what they are waiting on. A stage joins `stages_built` only when it exists and its invariant test passes. *(Rule 13)*
- **2026-08-31** — Removed a real maintenance hazard: the 40 phrasings and 20 high-stakes questions were duplicated between the invariant tests and the golden set. A phrasing corrected in one place would have silently stayed wrong in the other — and the copy that stayed wrong would be the one measuring whether INV-3 holds. Both now read `config/eval/golden_set.php`. *(Section 12)*
- **2026-08-31** — **The refusal / routing / citation core, and the first five invariant tests that actually pass.** `composer install` run for the first time (29 dev packages; the project still has **no runtime dependencies**). Built: `AnswerMode` (an enum whose `callsGenerator()` is the mechanical form of INV-2), `RefusalIntents` (individual-outcome and individual-record matching, run **before** retrieval so no context is ever fetched for those questions — INV-3), `CategoryRouter` (rule-based, deliberately not a classifier: a 97%-accurate classifier is 3% wrong about whether a question is about fees, and rules are auditable and correctable by the office that spots the mistake), `CitationBinder` (**discards, never repairs** — an answer with no citation, or one citing something outside the retrieved set, is refused whole), `Generator` (no tools, no function calling, no ability to act — OWASP "excessive agency") and `FakeGenerator` (records calls, so INV-2 can assert the generator was *never invoked* for a fees question). Tests: `NoIndividualOutcomeTest` (the 40 phrasings Section 12 mandates, **plus 15 negative cases** — refusal precision is measured too, and an assistant that refuses everything is not an assistant), `NoAnswerWithoutSourceTest`, `QuotedNotParaphrasedTest` (the 20 high-stakes questions Section 12 mandates), `NoHardDeleteTest`, `NoPortalIntegrationTest`. **156 tests, 375 assertions, all passing. PHPStan level 8 clean. PSR-12 clean.** *(Sections 2, 7, 8, 12)*
- **2026-08-31** — **The invariant tests immediately found five real defects in code written the same hour** — which is the argument for writing them from the specification's own mandated counts rather than from the implementation. (1) "Are my points enough for Education?" and "Is my aggregate good enough for engineering?" escaped INV-3: the pattern handled the declarative form ("my points are enough") but not interrogative inversion, which is the more natural phrasing. (2) "Can I see my admission letter?" matched *individual_outcome* on "can I … admission" and was routed to the Registry instead of the Portal — both refuse, so INV-3 held, but **a refusal that misdirects is a refusal that failed** (Section 9); fixed by checking the narrower record patterns before the broader outcome ones. (3) "How much does the course cost?" routed to **Grounded**, which would have sent a fees question through generation — the exact R-1 failure. Also fixed one wrong expectation of mine in a test (citation order is by reference number, not order of appearance). *(INV-2, INV-3, Section 9)*
- **2026-08-31** — Tooling made honest: PHPStan level 8 raised 7 real findings (unchecked `PDO::query()` results, which is `PDOStatement|false` even under `ERRMODE_EXCEPTION`) — **fixed rather than suppressed**, with the status page now reporting `null` for an unreadable count instead of a fabricated zero. Added `phpcs.xml.dist` documenting two deliberate deviations (side-effects rule excluded for `bin/` CLI entry points, which must run before any autoloader exists; 120-character warnings tolerated for regex and SQL literals, PSR-12's own soft limit) and `.gitattributes` normalising line endings, after CRLF from a scripted edit broke the PSR-12 check. *(CLAUDE.md Rule 10)*
- **2026-08-31** — Status page now shows the **real** state of all twelve invariants in three colour-coded bands — `tested` (5), `partial` (enforced in the schema or the grant table but with no test file: INV-7, INV-8, INV-11), `specified` (4) — rather than a single stale claim. Verified in the browser. *(Rule 13)*
- **2026-08-31** — **Corpus schema built: 7 forward-only migrations, 22 tables, applied and verified.** `0001_corpus` (offices, categories, documents, chunks with the embedding BLOB and the `FULLTEXT` index, chunk_codes, curated_entries, source_conflicts), `0002_interactions` (interactions, interaction_retrievals, interaction_citations, feedback, unanswered_questions), `0003_safety_and_budget` (rate_limits, rate_limit_violations, budget_periods), `0004_admin` (admin_users, admin_audit_log, ingest_runs), `0005_evaluation` (eval_questions, eval_runs, eval_results), `0006_seed_reference_data`, `0007_enforce_review_dates`. Design choices worth knowing: chunks **denormalise** `category_key`/`reviewed_at`/`is_authoritative`/`owning_office_id` down from the document so retrieval stays a single-table `FULLTEXT` scan with no join on the hot path; retrieved chunk IDs and scores are stored as **rows** (`interaction_retrievals`) not a JSON blob, so retrieval quality can actually be analysed and the threshold tuned against evidence rather than feel; `interaction_citations` makes INV-1 auditable after the fact (a `grounded` interaction with zero citation rows is by definition a violation); `budget_periods.ceiling_amount` is nullable so "not yet set" is representable and cannot be confused with zero, and must fail closed. **Seeded only what `requirements.md` itself names** — 3 offices and the 10 Section 7 categories, with all contact details `NULL`. A "Finance Department" row would have been a plausible guess about who owns fees content, and that is exactly the kind of guess this project must not make. Zero corpus content: Phase 0 gates indexing. *(Sections 3, 5, 6, 7, 11, 12, 13, 14)*
- **2026-08-31** — **Three least-privilege database accounts created and functionally verified — 26 probes, 26 passed** (`bin/verify_grants.php`, a repeatable script rather than a one-off check, per Rule 3's "attempt the write that should fail and record the result"). The design point: **no account on this schema is granted `DELETE` or `DROP`.** INV-12 therefore holds at three layers — review, the runner (which refuses any migration containing `DELETE`/`TRUNCATE`), and the server itself. Least privilege is also **purpose limitation** here: the ingestion worker has no access at all to `interactions`/`feedback`/`unanswered_questions`, because those are chat logs and personal data (DF-1) and ingestion has no purpose requiring them — verified, not assumed. Also verified: the app cannot write the corpus (a request path that could rewrite a fees chunk is a defacement risk), cannot `UPDATE` or `DELETE` the audit log (append-only by grant, so a compromised console session cannot rewrite history), can record a login but cannot change a role or a password hash (column-level grant), and **holds no privilege on `gu_hrms` or `gu_website`** — INV-10 made true in the grant table rather than merely intended. Accounts are split across `db/accounts_bootstrap.sql` (schema + migration account) and `db/accounts.sql` (table-level grants, run *after* migrations) — granting on `gu_aia.*` instead would be one line shorter and would hand both other accounts the run of the whole schema including the chat logs; the extra step is the point. *(CLAUDE.md Rule 3; INV-10, INV-12)*
- **2026-08-31** — **Real INV-11 bypass found and closed.** See the resume note at the top of this file for the full account: `reviewed_at DATE NOT NULL` did not prevent a document with no review date, because the dev server is not in strict `sql_mode` and substitutes `0000-00-00`. Found by *actually attempting the insert* rather than reading the DDL. Fixed with `CHECK` constraints (`0007`) plus a strict per-connection `sql_mode` (`config/pdo_options.php`, also setting `ATTR_EMULATE_PREPARES => false` so prepares are real). Re-tested afterwards: omitting `reviewed_at`, an explicit `0000-00-00`, and a zero `review_interval_days` are now all refused, and a properly-formed document still inserts. The probe rows were removed so the corpus is genuinely empty. *(INV-11)*
- **2026-08-31** — **Migration runner fixed after a real failure on first use.** It wrapped every migration in a transaction, but MySQL/MariaDB commit implicitly on every DDL statement, so `commit()` threw `There is no active transaction` and `0001` applied without its ledger row. Now the runner detects DDL and wraps only data-only migrations, which genuinely are atomic; DDL migrations run unwrapped and, on failure, say plainly that partial state may exist and that the fix is a new forward migration rather than an edit. Recorded in `db/migrations/README.md` too — the limitation is real and pretending otherwise would be worse. *(Section 3)*
- **2026-08-31** — Status page preflight rewired onto real state: connects as the **least-privilege app account** (not the migration account — the preflight should exercise the access the running application has), reports applied-vs-on-disk migrations from the ledger, seeded category count, and corpus size. **Corpus shows "Pending — 0 documents, 0 chunks — empty by design; Phase 0 gates indexing"**, deliberately not marked OK, so nobody reads an empty corpus as a working one. Browser pass caught two more real defects: the app account had no `SELECT` on `schema_migrations` (ledger showed "unreadable"), now granted read-only; and the table caption still claimed "no schema has been written", now corrected. *(Section 10; standing browser-check directive)*
- **2026-08-31** — **GU crest wired in (client supplied the logo files).** They were placed at `assets/images/` (four sizes: 32, 128, 180, and a 261 KB original) and were **not web-reachable** — the root `.htaccess` routes every request through `public/`, so anything served must live there; `assets/images/logo-180.png` returned `404`. Moved to `public/assets/images/`, which leaves the public URL identical. Wired as favicon (32, 128), apple-touch-icon (180), and a masthead lockup beside the title. **Second browser-pass catch:** the crest PNG has a transparent background and its outline, the "GULU UNIVERSITY" band and the "FOR COMMUNITY TRANSFORMATION" scroll are all dark green — against the dark green masthead gradient they effectively disappeared, and a drop-shadow halo was not enough. Fixed by placing the mark on a white disc, which is the light ground it is designed for, rather than recolouring or outlining a university crest. Verified by zooming in that the circular clip does not crop the top band or the bottom scroll. `alt=""` used deliberately — the crest carries the wordmark and motto that the adjacent `<h1>` and tagline already state in text, so descriptive alt text would make a screen reader announce the institution twice. Added `srcset`/`sizes` so a 1× display fetches the 29 KB 128px file instead of the 51 KB 180px one. Verified: `php -l` clean, all three icon URLs `200`, page `200` at 12 KB of HTML, zero console errors. *(Section 10; CLAUDE.md Rule 8)*
- **2026-08-31** — Repository initialised, committed, and pushed to `https://github.com/ojpeter/gu-aia.git` (client instruction). Verified before staging that `.env` was not present and nothing secret was entering history; 37 files in the initial commit. *(Housekeeping)*
- **2026-08-31** — Scaffolding: `.gitignore` (`.env` excluded), `.env.example` (three separate least-privilege DB accounts per Rule 3, `GENERATOR_DRIVER=fake` default, budget and retention keys present but empty), `composer.json` (PSR-4 `GuAia\`, PHPUnit/PHPStan/PHPCS dev deps, a `check:no-delete` script for INV-12), `phpunit.xml.dist` (invariant suite listed first as the release gate), `phpstan.neon.dist` (level 8), `README.md`. *(Sections 3, 4)*

---

## Pending / In-Progress Tasks (by phase, per `requirements.md` Section 15)

### Phase 0 — Content audit (gates all indexing; not an engineering task)
- [ ] Content audit with Communications and the Registry: one authoritative source per fact, owners and review dates assigned. **No indexing before this completes.** Section 15 warns it will take longer than the build.
- [ ] Confirm real refusal/handoff contacts (office, email, telephone) for `config/refusals.php` — currently `null` on purpose.
- [ ] Communications to author the real refusal copy (Section 9).

### Phase 1 — Public assistant
**Done:** corpus schema (7 migrations, 22 tables) and the three least-privilege accounts, verified functionally.

**Immediate next:**
- [ ] **Feedback (thumbs up/down + comment)** — Section 10 and DF-3. The `feedback` table exists; the UI and handler do not. Needs its own CSRF check and rate limit.
- [ ] **Set `LOG_RETENTION_DAYS`** with the University's data protection function and publish it in the privacy notice. The sweep is built and refuses to run without it; this is now the single blocking item before the widget could be exposed to the public. *(Section 18, open question 5)*
- [ ] **Have Communications review `config/prompts/system-v1.txt`** before it is ever used against real questions. Section 8 says it changes only by merge request; it has had no review at all yet.
- [ ] **Fetchers and extractors** — crawler restricted to the domain and an allow-list, PDF text extraction, scanned-PDF rejection with a report to the owning office. Note the ordering trap: this is the piece that *could* index content, and **Phase 0 must complete first.**
- [ ] **Blocked on the offices, not on engineering** — carry these into any status update rather than letting them sit: 82 more golden questions from the Registry and Communications; the Acholi and Luganda cases from a competent speaker; the real refusal contacts for `config/refusals.php`; and Phase 0 itself, which gates all indexing.
- [ ] The three invariant tests that still do not exist: `DisclosureTest` (INV-4) and `PayloadBudgetTest`/`NoJsFallbackTest` (INV-9), both blocked on the widget; and `ReviewedAtMandatoryTest` (INV-11), which is **not** blocked — it could be written today against the `CHECK` constraints in `0007`. INV-11's other half (every answer rendering its source's last-reviewed date) does need the widget; `AnswerResult::$staleSource` already carries the flag and is tested.
- [ ] Ingestion: fetchers, PDF extraction (reject scanned/no-text-layer), cleaner with instruction-stripping (INV-6), structure-aware chunker (never split a fees table), embedder.
- [ ] Retrieval: `FULLTEXT` candidate generation (bind the query; sanitise BOOLEAN MODE operators), exact code matching + boost, in-process cosine rerank, threshold, category filter.
- [ ] Answering: category router (refusal intents matched **before** retrieval), prompt builder, `Generator` interface + fake + real client, citation binder (discard, never repair).
- [ ] Safety: refusal intents, injection defences, rate limiting, budget guard + degraded mode as a tested path.
- [ ] Logging: `InteractionLogger` in the same transaction as the response, feedback, weekly Unanswered Questions Report.
- [ ] Widget: server-rendered shell with server-side AI disclosure (INV-4), under 60 KB total, no-JS fallback with its own tests, WCAG 2.1 AA, focus management on new answers.
- [ ] Admin console for Communications and the Registry (corpus browser, re-index trigger, curated Q&A, authoritative flag, conflicts, reports, last eval run). **No content editing beyond curated entries** — the website stays the source of truth.
- [ ] CI: invariant suite, eval harness blocking on regression, payload-weight check, `DELETE` grep, PHPStan.
- [ ] Privacy notice published and linked from the widget.
- [ ] Entry in the Register of Approved AI Tools under `DICTS/POL/AI/001`.
- [ ] Widget embed into `gu-website` — cross-project, see Cross-Project Notes.

### Phase 2 — Portal-authenticated assistant
- Explicitly **not begun in this repository**. A separate specification. INV-10 requires the integration surface to not exist here.

### Phase 3 — Staff assistant over the HR Manual and policies
- Section 15 note: **consider running Phase 3 before Phase 2** (lower risk, stable content, forgiving audience).

---

## Known Issues / Open Questions

From `requirements.md` Section 18, none yet resolved:

1. **Hosted API or self-hosted open-weight model** — interim: build behind the `Generator` interface so it stays reversible; start hosted with a hard cap.
2. **May query data leave Uganda, and what does the privacy notice say** — interim: assume it does, state it plainly. Blocks the privacy notice.
3. **Which languages at launch** — English at minimum; test Acholi and Luganda in the eval set and report honestly before claiming support.
4. **Monthly budget ceiling** — configuration, set by the Chief, ICT Services before launch. `config/budget.php` is `null` and must fail closed until set.
5. **Log retention period** — configuration, set with the University's data protection function. Blocks the privacy notice.
6. **Who owns the unanswered-question report** — proposed Communications, copied to the Registry. Not confirmed.

Additional, this project's own:

7. **Real refusal contacts unknown.** `config/refusals.php` has `null` emails/telephones deliberately — a refusal that routes a user to a dead end is worse than the refusal itself.
8. **Sponsor expectation on the "mirror effect" (Section 19, risk R-12).** The assistant will surface the website's contradictions and staleness at scale, in public. Section 19 is explicit that whoever sponsors this must understand that **before Phase 0 begins**. Not yet confirmed that they do.

---

## Standing User Directives (apply always, not just this session)

- **Implement and maintain industry standards on AI and Data Access, and everything else required** (client, 2026-08-31). Encoded as `CLAUDE.md` Rules 2, 3, 4, 5 and 11, with the three `docs/` artefacts as the maintenance mechanism. "Maintain" is the half that rots — update the register on events, not on a calendar.
- **Keep checking developed modules in a real browser using Claude in Chrome** (client, 2026-08-31). `curl` confirms bytes are served; it does not confirm what a browser renders, which CSS rule wins the cascade, how relative paths resolve, or whether a control is operable. **Applied on its first day and it paid for itself immediately** — it caught the white-on-yellow badge contrast failure described in the log above, which every non-browser check had passed. When the widget exists, the browser pass must additionally include a keyboard-only run and a check that the no-JS fallback works with JavaScript actually disabled (INV-9, Rule 8), not merely that the page loads. **Known tool limitation on this machine:** `resize_window` changes window height but not rendered viewport width, so narrow-viewport rendering cannot currently be confirmed this way — report that honestly instead of claiming a mobile check that did not happen.
- **SEO and full mobile responsiveness** apply to every GU web property (standing directives carried from `gu-website`) — relevant here to the widget and the admin console.

---

## Cross-Project Notes

- **`gu-website`** — the widget is embedded there (Section 10). When that happens it is a cross-project event: the CSP `frame-ancestors`/`X-Frame-Options` on both sides, the 60 KB payload budget counted against `gu-website`'s own Core Web Vitals budget (LCP < 2.5s, INP < 200ms, CLS < 0.1), and `gu-website`'s "never write a root-absolute internal path" rule all apply. Note any such change in **both** projects' `progress.md`. `WIDGET_ALLOWED_ORIGIN` in `.env` exists so the embed origin is configured, not assumed.
- **`gu-services`** — **no integration in Phase 1, deliberately** (INV-10). Do not add a bridge, a stub, or a "ready for Phase 2" surface. `gu-website` reads `gu-services` through its own bridge module; that pattern is **not** to be copied here yet, because the data classes are different — `gu-website` reads published CMS content, whereas Phase 2 here would reach personal student records, which needs its own specification and its own controls.
- Phase 3 (staff assistant over the HR Manual) would read `gu-services`-adjacent policy documents. Still a separate phase, still not started.
